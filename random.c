#include<stdio.h>
#include<string.h>

int main(){

  int randomNum = 1;

  printf(" 1 +=2 : %d\n", randomNum += 2);

  // shortcurt methodes are -=, *=, /=, %=, variableName++, variableName--,

  int exNume = 1;

  printf("++%d : %d\n\n", exNume, ++exNume);

  exNume = 1;

  printf("%d++ : %d\n\n",exNume, exNume++);

}
